package com.techhub.laptopinventory.app;

import javax.persistence.EntityManager;
import javax.persistence.EntityManagerFactory;
import javax.persistence.Persistence;

import com.techhub.laptopinventory.entity.Laptop;

public class FirstLevelCach {

    public static void main(String[] args) {
 
    	EntityManagerFactory  emf = Persistence.createEntityManagerFactory("rohan");
    	 EntityManager em1 = emf.createEntityManager();
    long start = System.currentTimeMillis();
    Laptop l1 = em1.find(Laptop.class, 1);
    long end = System.currentTimeMillis();
    System.out.println(end-start);
    Laptop l2 = em1.find(Laptop.class, 2);
    long start2 = System.currentTimeMillis();
    Laptop l3 = em1.find(Laptop.class, 1);
    long end2 = System.currentTimeMillis();
    System.out.println(end2-start2);
}
}